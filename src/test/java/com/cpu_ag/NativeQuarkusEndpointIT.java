package com.cpu_ag;

import io.quarkus.test.junit.SubstrateTest;

@SubstrateTest
public class NativeQuarkusEndpointIT extends QuarkusEndpointTest {

    // Execute the same tests but in native mode.
}